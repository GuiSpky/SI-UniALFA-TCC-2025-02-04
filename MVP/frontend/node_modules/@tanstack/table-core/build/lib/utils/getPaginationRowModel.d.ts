import { Table, RowModel, RowData } from '@tanstack/table-core/build/lib/types';
export declare function getPaginationRowModel<TData extends RowData>(opts?: {
    initialSync: boolean;
}): (table: Table<TData>) => () => RowModel<TData>;
